class Game:
    def round(current_player):
        round_score = 0
        current_player()
        points = self.die.roll() this score is between 1 and 6
             while points is not 1
                if current_player rolls dice:
                    add points to round_score
                    continue

    
